//
// Created by wnx on 2021/6/11.
//
#include <iostream>
#include "front/ast/AstNode.h"

using namespace compiler::front::ast;
using namespace std;


int main(int argc, char **argv) {
  /*AST *root = new AST();
  vector<Declare *> v;
  auto *a = new VarDeclareWithInit(Identifier("a"), new NumberExpression(5));
  v.push_back(a);
  auto *b = new VarDeclare(Identifier("b"));
  vector<Expression *> array;
  array.push_back(new NumberExpression(1));
  array.push_back(new NumberExpression(2));
  array.push_back(new NumberExpression(3));
  array.push_back(new NumberExpression(4));
  array.push_back(new NumberExpression(5));
//  auto c = Identifier("c", true,new NumberExpression(5));
//  auto *d = new ArrayDeclareWithInit(new NumberExpression(5),c,array);
  v.push_back(b);
  auto *stmt = new DeclareStatement(v);
  root->codeBlock.push_back(stmt);
  root->codeBlock.push_back(a);
  root->codeBlock.push_back(b);
//  root->codeBlock.push_back(d);
  root->print();
  return 0;*/
}